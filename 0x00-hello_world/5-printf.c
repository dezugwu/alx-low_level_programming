#include <stdio.h>

/**
 * main - print " ith proper grammar, but the outcome is a piece of art," followed by newline
 *
 * Return: Always 0 (success)
 *
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
